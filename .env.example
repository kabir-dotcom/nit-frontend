# Set this when deploying so the frontend knows where the backend lives.
# In local dev you can leave it commented to use the Vite proxy (`/api`).
# VITE_API_URL=https://your-backend.example.com/api

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
